import { useFollow } from '@/composables'
import type { IUser } from '@/types'

export function sortByFollow(list: IUser[]) {
  // const {isFollowing} = useFollow()
  // list.sort(async (a, b) => {
  //   const isAFollowingB = await isFollowing(a.id, b.id)
  //   const isBFollowingA = await isFollowing(b.id, a.id)
  // })
}
